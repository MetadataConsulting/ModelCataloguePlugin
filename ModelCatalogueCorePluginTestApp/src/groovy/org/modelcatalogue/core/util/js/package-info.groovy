/** Configures Javascript frontend with arbitrary JavaScript to be run after all external dependencies are added to the header but before the application
 * module is initialized with the main angular module metadataCurator. */
package org.modelcatalogue.core.util.js
